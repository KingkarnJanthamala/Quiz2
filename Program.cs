using System;

class Program {
    static void Main(string[] args) {
       Console.Write("Input N: ");
       int N = int.Parse(Console.ReadLine()); 

       Console.Write("Input K: ");
       int K = int.Parse(Console.ReadLine());

       Console.Write("Input gender: ");
       int gender = int.Parse(Console.ReadLine());

       Console.Write("Input score1: ");
       int score1 = int.Parse(Console.ReadLine()); //score1 จากกรรมการคนแรก ผู้ชาย

       Console.Write("Input score2: ");
       int score2 = int.Parse(Console.ReadLine()); //score2 จากกรรมการคนแรก ผู้หญิง

       if (N >= 1000)
    }
}